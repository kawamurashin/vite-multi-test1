{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "watch:page1": "vue-tsc --noEmit && vite build --mode development --watch --config config/vite.config.page1.ts",
    "watch:page2": "vue-tsc --noEmit && vite build --mode development --watch --config config/vite.config.page2.ts",
    "build:page1": "vue-tsc --noEmit && vite build --config config/vite.config.page1.ts",
    "build:page2": "vue-tsc --noEmit && vite build --config config/vite.config.page2.ts",
    "preview": "vite preview"
  },
  "dependencies": {
    "@types/node": "^17.0.29",
    "vue": "^3.2.25"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^2.3.1",
    "typescript": "^4.5.4",
    "vite": "^2.9.5",
    "vue-tsc": "^0.34.7"
  }
}
